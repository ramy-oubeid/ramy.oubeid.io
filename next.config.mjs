/** @type {import('next').NextConfig} */

const nextConfig = {
    output: 'export',
    images: {
        unoptimized: true,
    },
    // Add any other necessary configurations here
};

export default nextConfig;
